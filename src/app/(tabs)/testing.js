import { View } from "react-native";
import { EaseView } from "react-native-ease";

const testing = () => {
    return (
        <View>
            <EaseView></EaseView>
        </View>
    );
};

export default testing;
